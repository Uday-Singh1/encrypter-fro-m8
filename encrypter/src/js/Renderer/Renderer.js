class Renderer{
    render(whatToRender, whereToRender) {
       whereToRender.appendChild(whatToRender);   // waar ga je renderen, voeg daar aan toe wat je gaat renderen
    }
}

//de renderer rendered